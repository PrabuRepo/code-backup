package com.ravindrababu.problems;

public class BackTrackingAlgorithms {
	// Print all permutations of a given string.
	// Find subset of elements that are selected from a given set whose sum adds up to a given number K.
	/* Given a set of n integers, divide the set in two subsets of n/2 sizes each such that the difference of the sum of two
	 subsets is as minimum as possible.*/
	// Solve Sudoku using backtracking.
	// Given a maze, NxN matrix. A rat has to find a path from source to destination. Left top corner is the source and
	// right bottom corner is destination. There are few cells which are blocked, means rat can¬not enter into those cells.

}
